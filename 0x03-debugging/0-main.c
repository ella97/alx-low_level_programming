#include "main.h"

/**
 * main - tests function that prints if integer is positive or negative
 * Return: 0
 */

int main(void)
{
        int i;

        i = 0; // Change the value of i to 0 for testing
        positive_or_negative(i);

        return (0);
}
